schema: "./graphql-schema.json"
documents:
  - "src/**/*.graphql"
  - "src/**/*.{ts,tsx}"
generates:
  src/types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
