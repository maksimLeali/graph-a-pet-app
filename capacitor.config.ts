import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'graph-a-pet-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
