mutation updateUser($id: ID!, $data: UserUpdate!) {
	updateUser(id: $id, data: $data) {
		success
		error {
			code
			message
		}
		user {
			id
			first_name
			last_name
			email
			profile_picture {
				id
			}
		}
	}
}
