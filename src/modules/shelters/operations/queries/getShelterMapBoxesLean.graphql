query getShelterMapBoxesLean($id: ID!) {
	getShelterMap(id: $id) {
		success
		error {
			code
			message
		}
		map {
			id
			boxes {
				id
				label
				current_occupants {
					id
				}
			}
		}
	}
}
