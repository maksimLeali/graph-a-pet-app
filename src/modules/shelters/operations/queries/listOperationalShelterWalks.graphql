query listOperationalShelterWalks($shelter_id: ID!) {
	listOperationalShelterWalks(shelter_id: $shelter_id) {
		items {
			...MinShelterWalk
		}
		success
		error {
			code
			message
		}
		pagination {
			current_page
			page_size
			total_items
			total_pages
		}
	}
}
