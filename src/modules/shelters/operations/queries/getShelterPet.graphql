query getShelterPet($id: ID!) {
	getShelterPet(id: $id) {
		success
		error {
			code
			message
		}
		shelter_pet {
			id
			pet {
				id
				name
			}
			assigned_members {
				id
				first_name
				last_name
			}
			assigned_shelter_people {
				id
				first_name
				last_name
			}
		}
	}
}
