query listShelterTasks($commonSearch: CommonSearch) {
	listShelterTasks(commonSearch: $commonSearch) {
		items {
			...MinShelterTask
		}
		success
		error {
			code
			message
		}
		pagination {
			current_page
			page_size
			total_items
			total_pages
		}
	}
}
