query getPetWalkingStats($pet_id: ID!, $period: StatsPeriod!) {
	getPetWalkingStats(pet_id: $pet_id, period: $period) {
		success
		error {
			code
			message
		}
		chart {
			labels
			series {
				type
				data
			}
		}
	}
}
